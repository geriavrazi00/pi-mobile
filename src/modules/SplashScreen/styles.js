import { colors } from "_constants";

const container = {
  flex: 1,
  alignItems: "center",
  justifyContent: "center",
  backgroundColor: colors.DARK_BACKGROUND
};

const wave = {
  position: "absolute",
  bottom: 0,
  left: 0,
  right: 0
};

export { container, wave };
