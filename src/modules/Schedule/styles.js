import { colors } from '_constants';

const container = {
  flex: 1,
  alignItems: 'center'
};

const calendarScroll = {
  height: 70,
  backgroundColor: colors.PRIMARY,
  paddingTop: 10,
  paddingBottom: 10
};

export { container, calendarScroll };
