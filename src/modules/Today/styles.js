import { colors } from '_constants';

const container = {
  flex: 1,
  alignItems: 'center'
};

const topView = {
  alignSelf: 'stretch',
  backgroundColor: colors.PRIMARY,
  height: 70
};

export { container, topView };
